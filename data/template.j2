{% for entry in entries %}
  <h2>{{ entry.title }}</h2>
  <em>Written by {{ entry.author }}</em>
  {% if entry.image %}
    <img src="{{ entry.image }}">
  {% endif %}
  {{ entry.content[0].value | safe }}
{% endfor %}
